# 热点基金观察

一个 React + Vite + Node 的基金主题展示网站。前端展示热点主题、基金搜索、基金详情和风险提示；Node 服务提供 `/api` 代理并在生产环境托管 `dist` 静态文件。

## 本地开发

```bash
npm install
npm run dev
```

前端地址：`http://127.0.0.1:5173`

## 生产启动

```bash
npm run build
npm start
```

Render 部署配置见 `render.yaml`。
